.search-wrap {
  display: flex;
  flex-direction: row;
  align-items: flex-end;
}
.search-wrap input {
  border: 0;
  border-bottom: var(--border-height) solid var(--border-color);
  flex: 1;
  padding: 8rpx 20rpx;
  margin: 0 8rpx;
}
.search-wrap button {
  margin-right: 8rpx
}